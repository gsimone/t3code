# Sidebar terminal evidence

Recorded from `t3code/remove-terminal-drawer`, commit fba6638d4, after rebasing onto upstream main 18f7254e0.

[Watch the terminal demo (MP4)](terminal-sidebar.mp4).

- 0:00 — Cmd+J creates a sidebar terminal when the thread has none.
- 0:12 — Set a shell variable in that terminal.
- 0:24 — Hide the panel.
- 0:30 — Cmd+J reopens the existing terminal.
- 0:35 — Read the shell variable, confirming it is the same shell session.
- 0:43 — Cmd+J keeps the existing terminal open and focused.
- 0:50 — New Terminal creates a separate second tab.

`before.png` shows the drawer on upstream main. `after.png` shows the replacement sidebar terminal.

The video is a real browser recording with explanatory captions added below the app. No app frames were replaced or synthesized.
